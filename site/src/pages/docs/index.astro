---
import Docs from '@/layouts/docs.astro';
const guides=[['getting-started','Your first decision','Build, narrow a policy, compare allow and deny, and verify an audit chain.'],['concepts','Authorization & policies','Understand request inputs, Cedar semantics, schemas, and policy lifecycle.'],['deploy','Architecture & deployment','Choose embedded, sidecar, or shared PDP boundaries.'],['api','API, SDKs & CLI','Use current request formats and integration signatures.'],['identity','Identity & delegation','Separate standalone features from verification primitives.'],['audit','Audit & observability','Verify evidence, export records, and connect telemetry.'],['configuration','Configuration reference','Find settings and the exact surface that reads them.'],['operations','Operations & troubleshooting','Diagnose policy, network, identity, and audit failures.'],['security','Security model','Review enforcement responsibilities and current limitations.']];
---
<Docs title="Documentation"><div class="doc-title"><span class="section-kicker">AGENTGUARD / DOCUMENTATION</span><h1>Build an explicit<br/>authorization boundary.</h1><p>Start with a working local policy. Then choose your integration and make its trust boundaries visible.</p></div><h2>Build and operate</h2><div class="docs-grid">{guides.map(([path,title,body])=><a class="docs-card" href={`${import.meta.env.BASE_URL}docs/${path}/`}><h2>{title} ↗</h2><p>{body}</p></a>)}</div><h2>Project status</h2><p>These guides follow the checked-in implementation. Some README capabilities are library primitives or partially wired features; the guides call out that distinction. See <a href="https://github.com/sachncs/agent-guard/blob/master/CHANGELOG.md">the changelog</a>, <a href="https://github.com/sachncs/agent-guard#roadmap">roadmap</a>, and <a href="https://github.com/sachncs/agent-guard/tree/master/docs/internal/stages">historical implementation plans</a>.</p></Docs>
