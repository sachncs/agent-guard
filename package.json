{
  "name": "agentguard-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check:public": "node scripts/verify-public-surface.mjs",
    "check:docs": "node scripts/verify-doc-links.mjs"
  },
  "engines": {
    "node": ">=20.9"
  }
}
