//! AuthZEN HTTP + gRPC PDP server (sidecar mode).
//!
//! See `stages/STAGE-7-server.md` for the implementation plan.

#![allow(dead_code, unused_imports)] // stage-7 in progress

fn main() {
    eprintln!("agentguard-server: stub — implementation in stage-7");
    std::process::exit(1);
}