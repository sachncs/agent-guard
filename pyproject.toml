[project]
name = "agentguard"
version = "0.1.0"
description = "Cedar-powered authorization for AI agents"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.uv.workspace]
members = [
    "agentguard",
    "agentguard_langchain",
]